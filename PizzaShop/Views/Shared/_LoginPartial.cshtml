@if (Session["CurrentCustomerID"] != null)
{
    using (Html.BeginForm("Ausloggen", "Customer", FormMethod.Post, new { id = "logoutForm", @class = "navbar-nav" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav">
        <li>
            @Html.ActionLink("Hallo " + Session["CurrentCustomerName"].ToString() + "!", "Edit", "Customer", routeValues: null, htmlAttributes: new { title = "Profile" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav">
        <li>@Html.ActionLink("Registrieren", "Register", "Customer", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Login", "Login", "Customer", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
